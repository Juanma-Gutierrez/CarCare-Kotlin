<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/tb_it_userEmail"
        android:enabled="false"
        app:showAsAction="never"
        android:title="@string/label_username" />

    <item
        android:id="@+id/tb_it_settings"
        android:contentDescription="@string/settings_title"
        android:title="@string/settings_title"
        app:showAsAction="never" />

    <item
        android:id="@+id/tb_it_aboutMe"
        android:contentDescription="@string/settings_aboutMe"
        android:title="@string/settings_aboutMe"
        app:showAsAction="never" />

    <item
        android:id="@+id/tb_it_logout"
        android:contentDescription="@string/logout_title"
        android:title="@string/logout_title"
        app:showAsAction="never" />

</menu>